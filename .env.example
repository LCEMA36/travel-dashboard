# Copy this file to .env and fill in real values for LOCAL testing only.
# .env is git-ignored — it will never be committed or pushed to GitHub.
# In production (GitHub Actions), these same names are set as repo secrets
# instead: Settings -> Secrets and variables -> Actions.
AVIATIONSTACK_KEY=
TRIPIT_ICAL_URL=
